import type { Book } from "./types";

// TODO(you): CS-related books actually read, e.g.
// { title: "Designing Data-Intensive Applications", author: "Martin Kleppmann" }.
// Empty on purpose rather than guessed — Education renders nothing here until
// this has real entries, same as every other empty-data section on this site.
export const books: Book[] = [];
